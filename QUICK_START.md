# دليل التشغيل السريع - فعالية التنفيذي 🚀

## الخطوة 1️⃣: تثبيت المتطلبات

تأكد من تثبيت:
- Node.js v18 أو أحدث
- PostgreSQL v14 أو أحدث
- npm أو yarn

## الخطوة 2️⃣: تثبيت الحزم

```bash
cd tanfeethi-event
npm install
```

## الخطوة 3️⃣: إعداد قاعدة البيانات

### إنشاء قاعدة البيانات:
```bash
createdb tanfeethi_event
```

### تشغيل السكيما:
```bash
psql -d tanfeethi_event -f database/schema.sql
```

### التحقق من التثبيت:
```bash
psql -d tanfeethi_event -c "SELECT * FROM activity_status;"
```

## الخطوة 4️⃣: إعداد المتغيرات البيئية

```bash
cp .env.example .env
```

ثم عدّل `.env` بمعلوماتك:
```env
DB_USER=postgres
DB_PASSWORD=your_password
JWT_SECRET=tanfeethi_secret_2024
```

## الخطوة 5️⃣: إنشاء حساب أدمن

```bash
node scripts/create-admin.js admin Tanfeethi@2024 "المشرف الرئيسي"
```

سيظهر:
```
✅ تم إنشاء حساب المشرف بنجاح!
==========================================
اسم المستخدم: admin
كلمة المرور: Tanfeethi@2024
==========================================
```

## الخطوة 6️⃣: تشغيل التطبيق

### الطريقة الأولى (موصى بها):
```bash
npm run dev:all
```
هذا سيشغل Frontend و Backend معاً

### الطريقة الثانية (في نافذتين):
```bash
# نافذة 1 - Frontend
npm run dev

# نافذة 2 - Backend
npm run server
```

## الخطوة 7️⃣: الوصول للتطبيق

- **تطبيق الموظفين**: http://localhost:3000
- **لوحة التحكم**: http://localhost:3000/admin/login
- **API Backend**: http://localhost:3001

## الخطوة 8️⃣: اختبار التطبيق

### 1. تسجيل دخول الأدمن:
- افتح http://localhost:3000/admin/login
- اسم المستخدم: `admin`
- كلمة المرور: `Tanfeethi@2024`

### 2. اختبار تسجيل موظف:
- افتح http://localhost:3000/?uid=TEST001
- أدخل اسم وظيفة
- ستحصل على رقم من 1-200

### 3. تفعيل فعالية:
- من لوحة التحكم، فعّل "مرايا الهوية"
- سيظهر النشاط في خريطة الموظف

## ⚙️ إعدادات إضافية

### تغيير منفذ الخادم:
في `.env`:
```env
PORT=3001
```

### تغيير قاعدة البيانات:
في `.env`:
```env
DB_NAME=your_database_name
```

## 🎨 تخصيص الثيم

### تغيير الألوان:
في `tailwind.config.js`:
```javascript
colors: {
  tanfeethi: {
    brown: '#8B6F47',      // اللون الرئيسي
    turquoise: '#40E0D0',   // اللون الثانوي
    cream: '#F5F1E8',       // لون الخلفية
  },
}
```

### تغيير الشعار:
استبدل `/public/logo.png` بشعارك (مقاس موصى به: 500x500)

## 📱 إعداد QR Codes

### إنشاء روابط للموظفين:
```
http://localhost:3000/?uid=EMP001
http://localhost:3000/?uid=EMP002
...
```

### استخدام مولد QR:
- اذهب إلى https://qr-code-generator.com
- أدخل الرابط
- حمّل الصورة
- اطبع على السوار

## 🚨 حل المشاكل الشائعة

### مشكلة: "Cannot connect to database"
**الحل:**
```bash
# تحقق من تشغيل PostgreSQL
sudo service postgresql status

# ابدأ PostgreSQL
sudo service postgresql start
```

### مشكلة: "Port 3000 is already in use"
**الحل:**
```bash
# أوقف العملية
lsof -ti:3000 | xargs kill -9

# أو غيّر المنفذ
PORT=3002 npm run dev
```

### مشكلة: "Cannot find module"
**الحل:**
```bash
# أعد تثبيت الحزم
rm -rf node_modules package-lock.json
npm install
```

### مشكلة: "Unauthorized" في Admin
**الحل:**
```bash
# أعد إنشاء حساب الأدمن
psql -d tanfeethi_event -c "DELETE FROM admins WHERE username='admin';"
node scripts/create-admin.js admin NewPassword123
```

## 📊 مراقبة الأداء

### عدد المتصلين:
لوحة التحكم تعرض عدد المتصلين في الوقت الفعلي

### سجلات الخادم:
```bash
# مشاهدة سجلات Backend
npm run server
```

### قاعدة البيانات:
```bash
# عدد الموظفين المسجلين
psql -d tanfeethi_event -c "SELECT COUNT(*) FROM employees;"

# عدد الصور
psql -d tanfeethi_event -c "SELECT COUNT(*) FROM shared_photos WHERE is_approved = TRUE;"
```

## 🔒 أمان الإنتاج

قبل النشر في بيئة إنتاج:

1. ✅ غيّر JWT_SECRET إلى مفتاح قوي
2. ✅ استخدم HTTPS
3. ✅ فعّل CORS المحدد
4. ✅ أضف rate limiting
5. ✅ قيّد وصول قاعدة البيانات

## 📞 الدعم

للمساعدة:
- راجع `README.md` للتفاصيل الكاملة
- تحقق من السجلات في console
- افحص قاعدة البيانات

## ✅ قائمة التحقق قبل الفعالية

- [ ] تم اختبار جميع الفعاليات
- [ ] تم إنشاء 200 QR Code
- [ ] تم اختبار الكاميرا على أجهزة مختلفة
- [ ] تم اختبار Socket.io (الاتصال الحي)
- [ ] تم تجهيز الشاشات الكبيرة
- [ ] تم إنشاء نسخة احتياطية من قاعدة البيانات
- [ ] تم اختبار الأداء مع 50+ مستخدم متزامن

---

🎉 **الآن أنت جاهز لإطلاق فعالية رائعة!** 🎉
