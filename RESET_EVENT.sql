-- ═══════════════════════════════════════════════════════════
-- Reset Event Data - مسح بيانات الفعالية والبدء من جديد
-- ═══════════════════════════════════════════════════════════
--
-- ⚠️ تحذير: هذا الملف سيحذف جميع بيانات الموظفين والإجابات والصور
-- استخدمه فقط عندما تريد البدء من جديد في فعالية جديدة
--
-- كيفية الاستخدام:
-- 1. افتح Railway Dashboard
-- 2. اذهب إلى PostgreSQL → Query
-- 3. انسخ والصق الكود أدناه
-- 4. اضغط Execute
--
-- ═══════════════════════════════════════════════════════════

-- 1️⃣ مسح جميع الإجابات
DELETE FROM answers;
ALTER SEQUENCE answers_id_seq RESTART WITH 1;
-- ✅ تم مسح جميع الإجابات

-- 2️⃣ مسح جميع الصور المشاركة
DELETE FROM shared_photos;
ALTER SEQUENCE shared_photos_id_seq RESTART WITH 1;
-- ✅ تم مسح جميع الصور

-- 3️⃣ مسح جميع الإعجابات (Likes)
-- (سيتم مسحها تلقائياً مع الصور بسبب CASCADE)
-- ✅ تم مسح جميع الإعجابات

-- 4️⃣ مسح جميع الموظفين
DELETE FROM employees;
ALTER SEQUENCE employees_id_seq RESTART WITH 1;
-- ✅ تم مسح جميع الموظفين

-- 5️⃣ إعادة ضبط جميع الأسئلة إلى غير نشطة
UPDATE questions SET is_active = false;
-- ✅ تم إلغاء تفعيل جميع الأسئلة

-- 6️⃣ إعادة ضبط جميع الأنشطة إلى غير نشطة
UPDATE activity_status SET is_active = false;
-- ✅ تم إلغاء تفعيل جميع الأنشطة

-- ═══════════════════════════════════════════════════════════
-- النتيجة النهائية:
-- ═══════════════════════════════════════════════════════════
-- ✅ جميع الموظفين محذوفين
-- ✅ جميع الإجابات محذوفة
-- ✅ جميع الصور محذوفة
-- ✅ جميع الأنشطة معطلة
-- ✅ جميع الأسئلة معطلة
--
-- الآن يمكنك البدء من جديد! 🎉
-- ═══════════════════════════════════════════════════════════

-- عرض الإحصائيات بعد المسح:
SELECT
  'employees' as table_name,
  COUNT(*) as count
FROM employees
UNION ALL
SELECT
  'answers' as table_name,
  COUNT(*) as count
FROM answers
UNION ALL
SELECT
  'shared_photos' as table_name,
  COUNT(*) as count
FROM shared_photos
UNION ALL
SELECT
  'active_questions' as table_name,
  COUNT(*) as count
FROM questions
WHERE is_active = true
UNION ALL
SELECT
  'active_activities' as table_name,
  COUNT(*) as count
FROM activity_status
WHERE is_active = true;

-- يجب أن تكون جميع الأرقام = 0
