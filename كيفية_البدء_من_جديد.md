# 🔄 كيفية مسح البيانات والبدء من جديد

## ⚠️ تحذير مهم جداً!
هذه العملية ستحذف **جميع** بيانات الفعالية الحالية:
- ❌ جميع الموظفين المسجلين
- ❌ جميع الإجابات على الأسئلة
- ❌ جميع الصور المرفوعة
- ❌ جميع الإعجابات

**لا يمكن التراجع عن هذه العملية!**

---

## 📋 الخطوات (طريقتان)

### الطريقة 1️⃣: عبر Railway Dashboard (موصى بها)

1. **افتح Railway Dashboard:**
   - اذهب إلى: https://railway.app/dashboard
   - اختر مشروع `t-event-production`

2. **افتح PostgreSQL Query:**
   - اضغط على `PostgreSQL`
   - اختر تبويب `Query`

3. **نفذ الكود:**
   - افتح ملف `RESET_EVENT.sql`
   - انسخ **كل** محتوياته
   - الصقه في نافذة Query
   - اضغط `Execute` أو `Run Query`

4. **تحقق من النتيجة:**
   - يجب أن ترى جدول بالإحصائيات
   - كل الأرقام يجب أن تكون `0`

---

### الطريقة 2️⃣: مسح انتقائي (إذا أردت الاحتفاظ ببعض البيانات)

#### أ) مسح الموظفين فقط:
```sql
DELETE FROM employees;
ALTER SEQUENCE employees_id_seq RESTART WITH 1;
```

#### ب) مسح الإجابات فقط:
```sql
DELETE FROM answers;
ALTER SEQUENCE answers_id_seq RESTART WITH 1;
```

#### ج) مسح الصور فقط:
```sql
DELETE FROM shared_photos;
ALTER SEQUENCE shared_photos_id_seq RESTART WITH 1;
```

#### د) إلغاء تفعيل جميع الأسئلة:
```sql
UPDATE questions SET is_active = false;
```

#### هـ) إلغاء تفعيل جميع الأنشطة:
```sql
UPDATE activity_status SET is_active = false;
```

---

## 🎯 ماذا بعد المسح؟

### 1. تجهيز الأسئلة:
- اذهب إلى: `/admin/questions`
- فعّل السؤال الأول

### 2. تجهيز الأنشطة:
- اذهب إلى: `/admin/dashboard`
- فعّل الأنشطة التي تريدها:
  - ✅ مرايا الهوية (لرفع الصور)
  - ✅ تحدي بلا أخطاء (للأسئلة)
  - ✅ فن الإكرام
  - ✅ الصورة الجماعية

### 3. اختبار التطبيق:
- افتح: https://t-event-production.up.railway.app/
- سجل دخول موظف تجريبي
- تأكد أن كل شيء يعمل

---

## 📊 التحقق من البيانات

### لعرض عدد الموظفين:
```sql
SELECT COUNT(*) FROM employees;
```

### لعرض عدد الإجابات:
```sql
SELECT COUNT(*) FROM answers;
```

### لعرض عدد الصور:
```sql
SELECT COUNT(*) FROM shared_photos;
```

### لعرض الأسئلة النشطة:
```sql
SELECT * FROM questions WHERE is_active = true;
```

---

## 🔐 الأمان

### ملاحظات مهمة:
1. ✅ جدول `admins` لن يتأثر (بيانات تسجيل الدخول آمنة)
2. ✅ جدول `questions` لن يتأثر (الأسئلة محفوظة، فقط ستُلغى تفعيلها)
3. ✅ جدول `activity_status` لن يتأثر (الأنشطة محفوظة، فقط ستُلغى تفعيلها)
4. ⚠️ لا يمكن استرجاع البيانات المحذوفة

---

## 🎉 جاهز للبدء!

بعد تنفيذ الخطوات أعلاه:
- ✅ التطبيق جاهز لفعالية جديدة
- ✅ لا توجد بيانات قديمة
- ✅ جميع العدادات من الصفر
- ✅ الموظفون يمكنهم التسجيل من جديد

---

## 📞 دعم إضافي

إذا واجهت أي مشكلة:
1. تحقق من الـ Deploy Logs في Railway
2. تحقق من Database Logs
3. تأكد من تنفيذ جميع الأوامر بنجاح

**ملاحظة:** الملفات المرفوعة (الصور) في `/public/uploads`
ستبقى موجودة في الـ Volume، لكن لن يتم عرضها
لأن سجلاتها في قاعدة البيانات محذوفة.

---

**تاريخ الإنشاء:** 2024-12-24
**الإصدار:** v26
